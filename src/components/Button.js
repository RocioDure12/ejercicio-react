import "../styles/Button.scss";

const Button = ({content}) => {
    return (
        <div className="btns">
            <button className="">{content}</button>
        </div>
    )
}

export default Button;